import { RadioGroupComponent } from '@shared/components/radio/radio-group.component';

describe('RadioGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new RadioGroupComponent();
    expect(directive).toBeTruthy();
  });
});
