import { CountryFlagSrcPipe } from './country-flag-src.pipe';

describe('CountryFlagSrcPipe', () => {
  it('create an instance', () => {
    const pipe = new CountryFlagSrcPipe();
    expect(pipe).toBeTruthy();
  });
});
