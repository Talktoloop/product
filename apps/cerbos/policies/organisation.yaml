apiVersion: "api.cerbos.dev/v1"
resourcePolicy:
  version: "default"
  resource: "organisation"
  rules:
    - actions: ["read"]
      effect: "EFFECT_ALLOW"
      roles: ["MODERATOR", "CASE_MANAGER", "SUPER_ADMIN"]