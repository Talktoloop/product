apiVersion: "api.cerbos.dev/v1"
resourcePolicy:
  version: "default"
  resource: "user"
  rules:
    - actions: ["read"]
      effect: "EFFECT_ALLOW"
      roles: ["MODERATOR", "CASE_MANAGER", "SUPER_ADMIN"]