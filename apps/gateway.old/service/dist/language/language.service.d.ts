import { CheckTranslationAWSResponse } from './interface/check-translation-aws-response.interface';
import { LambdaClient, InvokeAsyncCommandOutput, InvocationResponse } from '@aws-sdk/client-lambda';
import { ComprehendClient } from '@aws-sdk/client-comprehend';
import { LanguageRepository } from './language.repository';
import { LanguageEntity } from './entity/language.entity';
import { SOURCE_TYPE } from '../common/constant/source-type.constants';
import { PROVIDER_TYPE } from './interface/provider.enum';
import { TranslationServiceClient } from '@google-cloud/translate';
import { google } from '@google-cloud/translate/build/protos/protos';
import { StoryTranslationModeratorService } from '../story/service/story-translation-moderator.service';
import { StoryTranslationEntity } from '../story/entity/story-translation.entity';
import { CommentTranslationEntity } from '../comment/entity/comment-translation.entity';
import { CommentTranslationModeratorService } from '../comment/service/comment-translation-moderator.service';
import { ConfigService } from '@nestjs/config';
export declare class LanguageService {
    private readonly config;
    private readonly awsTranslationProvider;
    private readonly googleTranslationProvider;
    private readonly lambdaProvider;
    private readonly languageRepository;
    private readonly storyTranslationModeratorService;
    private readonly commentTranslationModeratorService;
    private readonly logger;
    constructor(config: ConfigService, awsTranslationProvider: ComprehendClient, googleTranslationProvider: TranslationServiceClient, lambdaProvider: LambdaClient, languageRepository: LanguageRepository, storyTranslationModeratorService: StoryTranslationModeratorService, commentTranslationModeratorService: CommentTranslationModeratorService);
    checkTranslationByAWS(content: string): Promise<CheckTranslationAWSResponse>;
    getListOfProviders(first: string): Promise<string[]>;
    invokeTranslationModerator(sourceId: string, content: string, originalTextLangId: number, sourceType: SOURCE_TYPE, translations: StoryTranslationEntity[]): Promise<void>;
    invokeTranslation(sourceId: string, content: string, originalTextLangId: number, sourceType?: SOURCE_TYPE): Promise<void>;
    addTranslationError(sourceType: SOURCE_TYPE, sourceId: string, targetLang: LanguageEntity, content: string): Promise<void>;
    runTranslationLambda(sourceId: string, targetLang: LanguageEntity, content: string, originalTextLang: string, sourceType: SOURCE_TYPE, provider: PROVIDER_TYPE, alternativeProvider?: PROVIDER_TYPE): Promise<InvokeAsyncCommandOutput | void>;
    runTranslationLambdaSync(sourceId: string, targetLang: LanguageEntity, content: string, originalTextLang: string, sourceType: SOURCE_TYPE, provider: PROVIDER_TYPE, alternativeProvider: PROVIDER_TYPE): Promise<InvocationResponse>;
    getDefaultLanguage(): Promise<LanguageEntity>;
    getLanguageById(id: number): Promise<LanguageEntity>;
    getLanguageByCode(code: string): Promise<LanguageEntity>;
    getVisibleLanguages(): Promise<LanguageEntity[]>;
    getLanguages(): Promise<LanguageEntity[]>;
    checkProbabilityCriteria(language: string, content: string, provider: PROVIDER_TYPE): Promise<void>;
    checkTranslationByGoogle(content: string): Promise<google.cloud.translation.v3.IDetectedLanguage>;
    checkOriginLanguage(languageCode: string): Promise<LanguageEntity>;
    changeOriginLanguage(translations: (CommentTranslationEntity | StoryTranslationEntity)[], language: LanguageEntity, originalLanguageId: number, manualTranslationLanguages?: string[]): Promise<{
        translations: (CommentTranslationEntity | StoryTranslationEntity)[];
        contentUpdated: boolean;
        oldOriginalContent: string;
    }>;
}
