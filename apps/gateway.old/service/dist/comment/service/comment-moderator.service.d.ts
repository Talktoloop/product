import { UpdateResult } from 'typeorm';
import { CommentRepository } from '../repository/comment.repository';
import { CommentEntity } from '../entity/comment.entity';
import { COMMENT_STATUS } from '@ourloop/shared';
import { RejectReasonEntity } from '../../lexicon/entity/reject-reason.entity';
import { PaginationWithExtendedFilterDto } from '../../common/dto/pagination-with-extended-filter.dto';
import { RejectContentDto } from '../../common/dto/reject-content.dto';
import { LanguageRepository } from '../../language/language.repository';
import { UpdateCommentCommentDTO } from '../request/dto/update-comment.dto';
import { LanguageService } from '../../language/language.service';
import { LanguageEntity } from '../../language/entity/language.entity';
import { Pagination } from '../../common/type/pagination.type';
import { ThematicService } from '../../lexicon/service/thematic.service';
export declare class CommentModeratorService {
    private readonly commentRepository;
    private readonly languageRepository;
    private readonly languageService;
    private readonly thematicService;
    private readonly logger;
    constructor(commentRepository: CommentRepository, languageRepository: LanguageRepository, languageService: LanguageService, thematicService: ThematicService);
    changeOriginLanguage(comment: CommentEntity, language: LanguageEntity): Promise<CommentEntity>;
    updateComment(comment: CommentEntity, data: UpdateCommentCommentDTO): Promise<CommentEntity | void>;
    removeComment(id: string): Promise<CommentEntity>;
    getPendingComments(params: PaginationWithExtendedFilterDto): Promise<Pagination>;
    getCommentsByStatus(params: PaginationWithExtendedFilterDto, status: COMMENT_STATUS, withDetails?: boolean): Promise<Pagination>;
    rejectComment(comment: CommentEntity, rejectContent: RejectContentDto, rejectReasons?: RejectReasonEntity[]): Promise<CommentEntity>;
    unPublishComment(comment: CommentEntity): Promise<UpdateResult | void>;
    setCommentS3FileId(comment: CommentEntity, s3FileId: string): Promise<CommentEntity>;
    setCommentStatus(comment: CommentEntity, status: COMMENT_STATUS, restrictedStatuses?: string[]): Promise<CommentEntity>;
    publishComment(comment: CommentEntity): Promise<UpdateResult | void>;
    getCommentDetailsByIdOrFail(id: string): Promise<CommentEntity>;
}
