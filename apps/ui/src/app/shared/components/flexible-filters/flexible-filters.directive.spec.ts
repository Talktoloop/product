import { FlexibleFiltersDirective } from './flexible-filters.directive';

describe('FlexibleFiltersDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexibleFiltersDirective();
    expect(directive).toBeTruthy();
  });
});
