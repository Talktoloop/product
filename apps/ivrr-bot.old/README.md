# How to start

## development mode

1. Download project `https://gitlab.com/ourloop/backend/devops`
2. Go to directory with devops project and run command `docker-compose -f docker-compose-dev.yml up --build`
3. Go to directory with current project, install dependencies and run command `npm run start:dev`
4. TBD
